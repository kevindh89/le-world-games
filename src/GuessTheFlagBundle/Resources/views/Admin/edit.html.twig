{% extends 'base.html.twig' %}

{% block body %}
    <form method="post">
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Save</button>
        <a href="{{ path('admin_overview') }}" class="btn btn-default">Back</a>
    </form>
{% endblock %}
