{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <div>
            <img src="{{ photo_directory ~ form.vars.value.photo }}" alt="Photo" />
        </div>

        <button type="submit" class="btn btn-primary">Save</button>
        <a href="{{ path('admin_overview') }}" class="btn btn-default">Back</a>
    {{ form_end(form) }}
{% endblock %}
